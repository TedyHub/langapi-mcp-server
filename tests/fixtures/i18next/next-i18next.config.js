// Minimal next-i18next configuration so locale detection classifies this
// fixture as an i18next project (public/locales/<lang>/<namespace>.json layout).
module.exports = {
  i18n: {
    defaultLocale: "en",
    locales: ["en", "de"],
  },
};
